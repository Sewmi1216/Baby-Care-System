import { NicValidatorDirective } from './nic-validator.directive';

describe('NicValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new NicValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
