import { TextValidatorDirective } from './text-validator.directive';

describe('TextValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new TextValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
